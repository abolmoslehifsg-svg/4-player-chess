# bots/

پوشهٔ موتورها و ربات‌ها.

## Stockfish (۱v۱)
فایل `stockfish.js` را در ریشهٔ پروژه یا در `bots/stockfish/` بگذار و مسیر لود را در `js/game-1v1.js` تنظیم کن.

## ربات‌های ۲v۲
منطق AI چهارنفره در `js/ai-worker.js` است. ربات‌های جدید را اینجا اضافه کن و از UI وصل کن.
